# Active Todo Queue

Mission: keep the framework repo handoff-ready while preserving strict local validation.

1. Keep the local guardrail path green (`bash scripts/run_agent_request.sh` and `bash scripts/ci_local.sh`) before every handoff.
2. Add or update focused plan files under `docs/plans/active/` before starting non-trivial feature slices.
3. Move completed plan notes to `docs/plans/archive/` once their outcomes are reflected in `MEMORY.md` and stable docs.
