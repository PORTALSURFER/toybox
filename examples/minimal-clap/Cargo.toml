[package]
name = "toybox-minimal-clap"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[lib]
crate-type = ["cdylib"]

[dependencies]
toybox = { path = "../.." }

[build-dependencies]
toybox = { path = "../.." }

[lints]
workspace = true
